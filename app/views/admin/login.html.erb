<% flash.each do |key, value| %>
<div class="alert alert-<%= key %>"><%= value %></div>
<% end %>

<h1>Admin Login</h1>
<p>
    <%= form_tag do%>
        <%= "Key: "%><%= password_field_tag :key %><br>
        <%= submit_tag "Login" %><br>
    <% end %>
 
 <%= link_to "HomePage", controller: "welcome", action: "show" %>
</p>
